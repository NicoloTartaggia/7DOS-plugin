import { PanelCtrl } from 'grafana/app/plugins/sdk';

export class JsImportCtrl extends PanelCtrl {
    
  constructor($scope, $injector) {
    super($scope, $injector);
  }
  static template = "aaaaaaaaaaaaaaaa";
  link(scope, element) {
  }
  
}
