import {expect} from "chai";
describe("NetUpdater - iterator Test", () => {
  it("iterator Test", () => {
   /* const s: NetUpdater = new NetUpdater();
    let arrayTest=new Array<FluxResult> (new FluxResult(new ConcreteNodeAdapter(jsbayes.newGraph().addNode("test",['true', 'false']),[new BoolValue(true,"testValue")]), "true"));
    arrayTest.push(new FluxResult(new ConcreteNodeAdapter(jsbayes.newGraph().addNode("test2",['true', 'false']),[new BoolValue(true,"test2Value")]), "true"));
    
    let flux:FluxResults=new FluxResults(arrayTest);
    s.updateNet(flux);
    */
    expect(2).to.equal(2);
  });
});
