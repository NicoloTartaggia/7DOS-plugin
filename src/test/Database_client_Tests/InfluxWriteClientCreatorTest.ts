import { WriteClientCreator } from "../../core/database_client/WriteClientCreator";

import {expect} from "chai";

describe("WriteClientCreator - chooseClient", () => {
    it(" TODO ", () => {
        // TODO
        new WriteClientCreator();
        expect(true).to.equal(true);
    });
});
